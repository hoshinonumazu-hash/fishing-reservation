{
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "start": "next start",
    "lint": "next lint",
    "postinstall": "prisma generate",
    "vercel-build": "prisma generate && prisma migrate deploy && next build",
    "deploy:build": "npm install && npx prisma generate && npx prisma migrate deploy && npm run build",
    "deploy:start": "pm2 start npm --name fishing-app -- start",
    "deploy:restart": "pm2 restart fishing-app",
    "deploy:stop": "pm2 stop fishing-app",
    "deploy:logs": "pm2 logs fishing-app"
  },
  "prisma": {
    "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.1",
    "axios": "^1.12.2",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "next": "^15.5.4",
    "prisma": "^6.17.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-hook-form": "^7.64.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "24.7.0",
    "eslint": "^9.38.0",
    "eslint-config-next": "^16.0.0",
    "ts-node": "^10.9.2",
    "typescript": "5.9.3"
  }
}
